// The running code did not know its own version, which is the first thing
// any bug report needs. Kept in step with PluginConfig.json by a test that
// reads both and fails if they diverge — so a release bump cannot silently
// leave this behind.
export const APP_VERSION = '1.0.29';
export const APP_VERSION_CODE = '338';
